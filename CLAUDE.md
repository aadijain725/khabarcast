@AGENTS.md

## current direction (2026-04-25)

pivot to MAAS shape. agent org: manager → curator + researcher + (ideology agents × N) + composer + voice. target L3 across rubric. plan + agent diagram: `tasks/todo.md` phase 4. existing helpers (`doFetch`, `doGenerate`, `doRender`) become tools the agents call.

**slot architecture:** dialogue still has 2 speaker slots (KALAM, ANCHOR). hosts table contains the personalities that fill each slot. user picks 1 host per slot per episode. expanding to N speakers is a future migration.

<!-- convex-ai-start -->
This project uses [Convex](https://convex.dev) as its backend.

When working on Convex code, **always read `convex/_generated/ai/guidelines.md` first** for important guidelines on how to correctly use Convex APIs and patterns. The file contains rules that override what you may have learned about Convex from training data.

Convex agent skills for common tasks can be installed by running `npx convex ai-files install`.
<!-- convex-ai-end -->
